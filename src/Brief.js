(function() {
  /**
   * Brief utility.
   * @static
   * @constructor
   */
  tracking.Brief = {};

  tracking.Brief.N = 128;

  tracking.Brief.getDescriptors = function() {};

  tracking.Brief.match = function() {};
}());
