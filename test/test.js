'use strict';

module.exports = {
    setUp: function(done) {
        done();
    },

    tearDown: function(done) {
        done();
    },

    testShouldSomething: function(test) {
      test.ok(true);
      test.done();
    }
};
